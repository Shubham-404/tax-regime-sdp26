# ─── REQUIRED ────────────────────────────────────────────────────────────────

# Google Gemini API key
# Get it at: https://aistudio.google.com/app/apikey
# Revoke at:  https://aistudio.google.com/app/apikey (delete the key)
GEMINI_API_KEY=your_gemini_api_key_here

# URL of the locally running Chroma vector DB
# Start Chroma with: chroma run --path ./chroma_data
CHROMA_HOST=http://localhost:8000

# Backend server port
PORT=3000

# ─── n8n WEBHOOK ─────────────────────────────────────────────────────────────

# The URL of the n8n webhook node (after importing n8n_workflow.json)
# Example: http://localhost:5678/webhook/tax-report
N8N_WEBHOOK_URL=http://localhost:5678/webhook/tax-report

# ─── OPTIONAL: Google Drive (leave blank to save PDFs locally) ───────────────

# Path to Google service-account JSON credentials file
# Revoke at: https://console.cloud.google.com/iam-admin/serviceaccounts
# GOOGLE_APPLICATION_CREDENTIALS=./google-credentials.json
# GOOGLE_DRIVE_FOLDER_ID=your_drive_folder_id

# ─── OPTIONAL: Email via SMTP ─────────────────────────────────────────────────

# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your@email.com
# SMTP_PASS=your_app_password
# SMTP_FROM=noreply@taxexplainer.com
# SMTP_TO=recipient@email.com
